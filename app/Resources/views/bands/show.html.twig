{% extends 'base.html.twig' %}

{% block body %}
    <h1>Band</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ band.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ band.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ band.description }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('bands_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('bands_edit', { 'id': band.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
