{% extends 'base.html.twig' %}

{% block body %}
    <h1>Albums list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Releasedate</th>
                <th>Description</th>
                <th>Albumcover</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for album in albums %}
            <tr>
                <td><a href="{{ path('albums_show', { 'id': album.id }) }}">{{ album.id }}</a></td>
                <td>{{ album.name }}</td>
                <td>{{ album.releaseDate }}</td>
                <td>{{ album.description }}</td>
                <td>{{ album.albumCover }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('albums_show', { 'id': album.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('albums_edit', { 'id': album.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('albums_new') }}">Create a new album</a>
        </li>
    </ul>
{% endblock %}
