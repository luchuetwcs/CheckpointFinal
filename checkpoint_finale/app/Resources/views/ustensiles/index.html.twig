{% extends 'base.html.twig' %}

{% block body %}
    <h1>Ustensiles list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for ustensile in ustensiles %}
            <tr>
                <td><a href="{{ path('ustensiles_show', { 'id': ustensile.id }) }}">{{ ustensile.id }}</a></td>
                <td>{{ ustensile.nom }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('ustensiles_show', { 'id': ustensile.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('ustensiles_edit', { 'id': ustensile.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('ustensiles_new') }}">Create a new ustensile</a>
        </li>
    </ul>
{% endblock %}
