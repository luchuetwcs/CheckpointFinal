{% extends 'base.html.twig' %}

{% block body %}
    <h1>Ustensile</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ustensile.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ ustensile.nom }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('ustensiles_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('ustensiles_edit', { 'id': ustensile.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
