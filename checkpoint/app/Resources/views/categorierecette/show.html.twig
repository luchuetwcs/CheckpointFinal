{% extends 'base.html.twig' %}

{% block body %}
    <h1>Categorierecette</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorieRecette.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ categorieRecette.type }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('categorierecette_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('categorierecette_edit', { 'id': categorieRecette.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
